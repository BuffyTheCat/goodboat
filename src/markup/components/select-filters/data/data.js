var data = {
    select-filters: {
        service: {
            className: 'select-filters_service'
        },
        transport: {
            className: 'select-filters_transport'
        }
    }
}
